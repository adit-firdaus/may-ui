/** Semantic colour role shared by Button, Badge, Tag, Alert and friends. */
export type MayTone = 'brand' | 'neutral' | 'success' | 'warning' | 'danger' | 'info'

/** Control size shared by every interactive component. */
export type MaySize = 'sm' | 'md' | 'lg'
