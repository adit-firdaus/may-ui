{
  "name": "mayui-examples",
  "private": true,
  "type": "module",
  "description": "The unified May UI documentation, component catalog, patterns, and provider playground.",
  "scripts": {
    "dev": "npm --prefix .. run site",
    "build": "npm --prefix .. run site:build",
    "preview": "npm --prefix .. run site:preview"
  }
}
