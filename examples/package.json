{
  "name": "mayui-examples",
  "private": true,
  "type": "module",
  "description": "Gallery of May UI example screens. Not published — it exists to browse the examples outside Storybook.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
